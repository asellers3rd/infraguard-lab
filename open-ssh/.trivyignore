# Trivy ignore rules added here by the InfraGuard agent.
# Each entry should include a justification comment on the line above.
# Web servers require unrestricted egress to reach public package mirrors, APIs, and AWS service endpoints; locking down all outbound traffic would break normal operations.
AWS-0104
# VPC Flow Logs are a monitoring best-practice but enabling them requires additional IAM roles and CloudWatch resources outside the scope of this targeted SSH-ingress remediation.
AWS-0178
